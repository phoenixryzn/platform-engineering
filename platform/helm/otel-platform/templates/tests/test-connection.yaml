apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "otel-platform.fullname" . }}-test-connection"
  labels:
    {{- include "otel-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "otel-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
