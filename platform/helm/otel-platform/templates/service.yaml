apiVersion: v1
kind: Service
metadata:
  name: {{ include "otel-platform.fullname" . }}
  labels:
    {{- include "otel-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "otel-platform.selectorLabels" . | nindent 4 }}
