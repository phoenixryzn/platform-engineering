# otel-platform — Internal Helm wrapper chart
# All runtime configuration is delegated to the upstream opentelemetry-demo chart.
# Override values per environment using values-dev.yaml, values-stage.yaml, values-prod.yaml.
# Keys must be nested under "opentelemetry-demo:" to pass through to the sub-chart.

opentelemetry-demo: {}